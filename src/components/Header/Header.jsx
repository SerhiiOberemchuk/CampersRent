import { NavLink } from "react-router-dom";

const Header = () => {
  return (
    <header>
      <nav>
        <NavLink />
        <NavLink />
        <NavLink />
      </nav>
    </header>
  );
};

export default Header;
